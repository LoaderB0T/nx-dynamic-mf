{
  "executors": {
    "construct": {
      "implementation": "./impl",
      "schema": "./schema.json",
      "description": "Calculates hashes for the bundled modules remoteEntry.json & adds them to the module json file."
    }
  }
}
