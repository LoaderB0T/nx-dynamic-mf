{
  "executors": {
    "construct": {
      "implementation": "./impl",
      "schema": "./schema.json",
      "description": "Calculates hashes for the bundled modules remoteEntry.js & adds them to the module json file."
    }
  }
}
